# MonkMode backend — copy to ".env" and fill in.
# Any Postgres works: Vercel Postgres, Neon, Supabase, Railway, local docker.
# Neon example:
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
