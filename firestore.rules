rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Each user can read/write only their own state document.
    match /userState/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
